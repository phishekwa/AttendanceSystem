<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="petrusproject.com.attendancesystem">

    <supports-screens
        android:largeScreens="true"
        android:normalScreens="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <!--
        <activity android:name=".Take_Attendance" />
        <activity android:name=".AttendanceView" />
        <activity android:name=".Record_Marks" />
        <activity android:name=".View_Marks_Records" />
        -->
        <activity android:name=".Assessment" />
        <activity android:name=".AssessmentView" />
        <activity android:name=".Attendance" />
        <activity android:name=".AttendanceView"></activity>
    </application>

</manifest>